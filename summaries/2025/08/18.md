# Activity Summary for 8/18/2025

## 1:41:16 PM
The log shows multiple revisions to a `projects.json` file, detailing a portfolio of projects.  The `projects.json` file underwent several modifications between 12:43 PM and 1:07 PM on August 18, 2025.  These changes primarily involved adding and reordering project entries.  No projects were removed.  A new project, "Health Bot," was added around 12:47 PM.  There was also a brief period where the `projects.json` was incorrectly formatted (around 1:04 PM) before being corrected to a proper JSON array structure at 1:05 PM.  The final version of `projects.json` at 1:07 PM contained 16 project entries.  Each project is described with a title, description, image path, links (to GitHub and/or a demo), and relevant tags.  Many projects use Python, indicating a focus on this language.  Frequent tags include "Machine Learning," "Chatbot," "Web Development," and various frameworks like Flask and FastAPI.  Additionally, a `script.js` file was updated at 12:53 PM, containing Javascript code for a webpage, specifically initializing a text-based animation using typed.js and implementing smooth scrolling functionality for a navbar.


## 2:41:17 PM
The log shows several updates to the `exp.json` and `projects.json` files, which appear to contain data for a portfolio website, and  `edu.json` which contains educational information.  Changes to `exp.json` between 1:51 PM and 2:05 PM reflect refinements to the description of the internships.  Specifically, the intern role at Sunworks Consultants was changed from "AI/ML Development Intern" to "AI Intern," and the IDEMIA internship entry was modified,  and then eventually removed and replaced by a Unified Mentor internship. The image links for Sunworks Consultants were also updated. The company name for one IDEMIA entry was even briefly changed to Unified Mentor then replaced back with IDEMIA.

The `projects.json` file was updated between 2:12 PM and 2:12:57 PM. The only significant change is the removal of "Llama-3.1-8b-Instruct" from the tags for the "Multi-Lingual Sentiment Analysis" project, suggesting a simplification or change in the project's technology description.

The `edu.json` file was updated between 2:13 PM and 2:14 PM.  A "country" field was added to each education entry specifying "India".

The `exp.js` and `edu.js` files are Javascript files that dynamically generate the experience and education sections of the website respectively, based on the data loaded from corresponding JSON files (`exp.json`, `edu.json`).  Both files remained largely unchanged, indicating updates to the presentation of the data are independent of the structure and logic of the javascript code.  The changes made to the Javascript files were to incorporate the `country` field in `edu.js`.


## 9:02:31 PM
The log shows a development process focusing on building a question-answering system using embeddings and a vector database.  The initial version used FAISS, but later switched to Qdrant. The `embedder.py` file, responsible for document loading and embedding generation using Sentence Transformers, underwent several minor revisions, primarily concerning chunk ID formatting and a minor fix incrementing the `sno` variable within the `process_file` function.  The `main.py` file evolved from using FAISS to Qdrant for vector storage;  further modifications included enhancing output to display scores along with document IDs, and finally temporarily disabling the LLM response generation (`call_llm`). A `rough.py` file was created and extensively modified to perform chunk reconstruction quality audits using ROUGE and BLEU scores and to simulate query retrieval, displaying top-3 results and their cosine similarity scores. Initially, this file lacked imports and contained commented-out code before becoming fully functional. Finally, a `.gitignore` file was updated to include new generated files and folders.  The significant changes occurred between 4:34 PM and 5:52 PM on August 18, 2025, with a final cleanup and addition of files to `.gitignore` at 5:52 PM.  The `all-MiniLM-L6-v2` SentenceTransformer model was consistently used throughout the development.


## 10:02:18 PM
The `grade.py` file underwent several revisions between 9:28 PM and 9:42 PM on August 18, 2025.  The core functionality remained consistent: a `p(n)` function assigns grades based on numerical input, and several code blocks calculate and print grades ('Gen ai', 'mlops', initially 'CF', and finally 'gt') using different weighted formulas.  The primary changes involved modifying the grading formulas and variable names within these blocks, as well as updating the print statements to use the correct variable names and accurately reflect the changes in the calculation. There were also some minor input value adjustments.  Specifically, the 'CF' block was renamed to 'mlops', and the 'DLP' block was ultimately changed to 'gt'. The final revision included a summary comment at the end.

The `audit2.py` file was modified between 9:47 PM and 10:02 PM on August 18, 2025. This Python script performs a quality audit on document chunks.  The initial version (9:47 PM)  simply printed a summary of chunk issues. Subsequent versions added complete code. The script loads documents, chunks them, and then audits them based on length, sentence boundary alignment, semantic similarity (using ROUGE and BLEU scores), and overlap with preceding chunks.  The audit results are initially printed to the console; later versions write these results to an "audit_chunk.md" file.  There was one revision that altered the minimum and maximum acceptable lengths of the chunks.  The script uses several libraries including `nltk`, `rouge_score`, and a custom `retriever.embedder` module (which is assumed to handle document loading and chunking). The final version corrected an error in writing the results to a file by changing `open.file()` to `open()`.
