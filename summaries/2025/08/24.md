# Activity Summary for 8/24/2025

## 6:10:15 PM
The log shows the development of a FastAPI application for deploying a machine learning model.

The `.gitignore` file was created at 5:15:30 PM, ignoring the `.venv` directory.  The `requirements.txt` file was updated multiple times between 5:30:39 PM and 5:56:43 PM, adding various libraries such as `shap`, `pandas`, `scikit-learn`, `matplotlib`, and `fairlearn`.  Finally,  `fastapi[standard]` was added, indicating a shift towards using FastAPI for the API.

A `Dockerfile` was created at 5:56:25 PM, defining a Docker image to package the application, using a Python 3.10 slim image and installing dependencies from `req.txt` (presumably a typo, meant to be `requirements.txt`). The image exposes port 8200 and runs a uvicorn server.

Deployment configuration was set up at 5:58:03 PM via a `deploy.yml` file, utilizing GitHub Actions for deployment to Google Kubernetes Engine (GKE). This involves authentication with Google Cloud, setting up the Google Cloud SDK, configuring Docker with Artifact Registry, getting GKE credentials, and running a deployment script (`cml_deploy.sh`). This script builds, tags, and pushes a Docker image to Artifact Registry and then deploys it to GKE using a Kubernetes deployment YAML file (`k8s-deployment.yaml`). The deployment targets the `iris-cluster-6` cluster in the `us-central1` zone of the `eternal-cycling-463617-f7` project.

The `main.py` file underwent several revisions between 5:59:58 PM and 6:06:40 PM. Initially, it was a simple Iris classifier API.  Subsequent changes drastically altered the input schema (`Input` class), changing from an Iris-specific schema to a more comprehensive one with fields like `age`, `gender`, `cp`, `trestbps`, etc. The API title was changed to “OPPE2 API”. The final version of `main.py` takes an input with multiple features, implying a change in the underlying model used from an Iris classifier to something different. The changes likely reflect the model’s input features. The final version contained a `sno` field (serial number), suggesting the input data may be tabular.  A sample `payload.json` file at 6:10:05 PM showcases the expected JSON input format for the final API.


In summary, the log shows the iterative development and deployment of a FastAPI application, starting with a basic Iris classification model and evolving into a more complex predictive model with a significantly different input structure.  The entire process, from development to deployment, was automated using Docker and Kubernetes, facilitated by GitHub Actions.


## 7:10:17 PM
The log shows development of an MLOps project, focusing on deploying a FastAPI model.  The process involved creating a POST request using Lua and wrk for load testing, building a FastAPI application to serve the model, and finally deploying it to Google Kubernetes Engine (GKE).

**File-Specific Updates:**

* **`requirements.txt`:** This file evolved to include necessary libraries. Initially, it contained `shap`, `pandas`, `scikit-learn`, `matplotlib`, `fairlearn`, `fastapi[standard]`, and `wrk`. Later, `requests` and then `evidently` were added, suggesting progressive addition of monitoring and simulation capabilities.

* **`post.lua`:**  This file defines the POST request for load testing. It was significantly revised to generate random input data, improving the realism of load tests.

* **`wrk_output.md`:** This file records the output of the `wrk` load tests.  Multiple entries show consistent load test runs against the `http://172.23.96.1:8000/predict/` endpoint.  There are slight variations in the results between runs, likely due to the changes in the `post.lua` script and resulting load generation.

* **`main.py`:** This file contains the FastAPI application code. It loads a `model.joblib` model and defines an API endpoint (`/predict/`) that receives input data, makes predictions, logs the prediction time, and returns the result.  Logging is set up to record prediction times.


* **`sim_req.py`:** This script simulates requests to the `/predict/` endpoint. It initially printed individual results but was later modified to collect results, save them into a CSV (`predict.csv`), and enhance output formatting. The final version saves sample data as JSON strings within the CSV, indicating a focus on preserving detailed data for later analysis.

* **`.github/workflows/deploy.yml`:** This file defines a GitHub Actions workflow for deploying the application to GKE. It uses Google Cloud SDK and Docker to build and deploy, indicating automated deployment practices.


**Timestamps of Significant Changes:**

* **6:23:39 PM:**  Significant revision of `post.lua` to include randomized input data for improved load testing.
* **6:28:03 PM:** Completion of the FastAPI application (`main.py`).
* **6:28:33 PM, 6:33:18 PM, 6:35:39 PM:** Iterative improvements to the simulation script (`sim_req.py`), adding data logging and CSV output.
* **6:59:57 PM:** Deployment workflow configuration completed (`deploy.yml`).


**Patterns and Recurring Elements:**

* Consistent use of `wrk` for load testing throughout the development process.
* The `predict` endpoint (`/predict/`) remains the central point of the API.
* Iterative development is apparent, with multiple modifications to `post.lua` and `sim_req.py`.
* The project demonstrates a focus on model deployment and monitoring using tools like `wrk` and `evidently`. The incorporation of `evidently` suggests a move toward production monitoring of the model performance.
