# Activity Summary for 8/24/2025

## 6:10:15 PM
The log shows the development of a FastAPI application for deploying a machine learning model.

The `.gitignore` file was created at 5:15:30 PM, ignoring the `.venv` directory.  The `requirements.txt` file was updated multiple times between 5:30:39 PM and 5:56:43 PM, adding various libraries such as `shap`, `pandas`, `scikit-learn`, `matplotlib`, and `fairlearn`.  Finally,  `fastapi[standard]` was added, indicating a shift towards using FastAPI for the API.

A `Dockerfile` was created at 5:56:25 PM, defining a Docker image to package the application, using a Python 3.10 slim image and installing dependencies from `req.txt` (presumably a typo, meant to be `requirements.txt`). The image exposes port 8200 and runs a uvicorn server.

Deployment configuration was set up at 5:58:03 PM via a `deploy.yml` file, utilizing GitHub Actions for deployment to Google Kubernetes Engine (GKE). This involves authentication with Google Cloud, setting up the Google Cloud SDK, configuring Docker with Artifact Registry, getting GKE credentials, and running a deployment script (`cml_deploy.sh`). This script builds, tags, and pushes a Docker image to Artifact Registry and then deploys it to GKE using a Kubernetes deployment YAML file (`k8s-deployment.yaml`). The deployment targets the `iris-cluster-6` cluster in the `us-central1` zone of the `eternal-cycling-463617-f7` project.

The `main.py` file underwent several revisions between 5:59:58 PM and 6:06:40 PM. Initially, it was a simple Iris classifier API.  Subsequent changes drastically altered the input schema (`Input` class), changing from an Iris-specific schema to a more comprehensive one with fields like `age`, `gender`, `cp`, `trestbps`, etc. The API title was changed to “OPPE2 API”. The final version of `main.py` takes an input with multiple features, implying a change in the underlying model used from an Iris classifier to something different. The changes likely reflect the model’s input features. The final version contained a `sno` field (serial number), suggesting the input data may be tabular.  A sample `payload.json` file at 6:10:05 PM showcases the expected JSON input format for the final API.


In summary, the log shows the iterative development and deployment of a FastAPI application, starting with a basic Iris classification model and evolving into a more complex predictive model with a significantly different input structure.  The entire process, from development to deployment, was automated using Docker and Kubernetes, facilitated by GitHub Actions.
