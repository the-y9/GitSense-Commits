# Activity Summary for 2/6/2025

## 1:00:38 PM
The log shows development of a notebook (`DLP_Week_2.ipynb`) focused on tokenization, likely for a natural language processing task.  The significant activity happened between 12:34 PM and 12:59 PM on February 6th, 2025.

`DLP_Week_2.ipynb` underwent numerous iterative changes.  Initially, the notebook experimented with defining a tokenizer using  Byte-Pair Encoding (BPE), specifying a normalizer (LowerCase), pre-tokenizer (WhiteSpace), and special tokens ([GO],[UNK],[PAD],[EOS]).  The example text "SEBI study finds 93% of individual F&O traders made losses between FY22 and FY24." was used for testing.

The code went through several iterations to define and train the tokenizer. There were attempts to load a dataset (`bookcorpus`) which appears to have encountered an error (although the exact error message is not shown). The code involves splitting the text, calculating token lengths, and ultimately training the BPE tokenizer using a batch-processing approach with a progress bar (`tqdm`). The final successful training of the tokenizer utilizes the `train_from_iterator` method with correctly specified `length` parameter (previously omitted).  There were also several instances of adding or removing code blocks, suggesting experimentation and refinement of the code.  The `COMMIT_EDITMSG` file indicates that other files (`README.md` and `DLP_Week_1.ipynb`) were also modified but their content is not shown in the log.


## 3:00:38 PM
The log shows a series of code changes primarily focused on tokenization within a Deep Learning Practices project.  The `DLP_Week_2.ipynb` notebook underwent extensive modification between 1:00 PM and 2:58 PM on February 6th, 2025.  These changes involved experimentation with Byte Pair Encoding (BPE) tokenizers, specifically adjusting vocabulary sizes (10K, 15K, 32K) and retraining the model multiple times.  The code iteratively trains a tokenizer using different vocabulary sizes, printing the resulting token lengths for a sample text.  There's also exploration of using a pre-trained tokenizer ("hopper.json") and manually adding tokens ("FY") to the vocabulary.  The `README.md` file was updated to reflect the week 2 summary, covering tokenization techniques. The `requirements.txt` file was updated to include `jupyter-book` and `pandas` libraries.  Finally, a commit message indicates several new files related to the "hopper" tokenizer model were added.  The final section of the log shows the loading and use of the `stanfordnlp/imdb` dataset for evaluating tokenizers including those that were newly trained and a pretrained one.
