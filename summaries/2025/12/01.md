# Activity Summary for 12/1/2025

## 2:34:46 PM
The code changes log outlines the development and refinement of document extraction utilities in Python, primarily focusing on `.docx` files and handling both English and Chinese content. All noted changes occurred on December 1, 2025, within a short timeframe, indicating active development.

### File-Specific Updates:

1.  **`d:\air\src\core\extractor_updated.py` (Timestamp: 12/1/2025, 1:46:07 PM)**
    This file represents a comprehensive, function-based Python module (`doc_to_json_updated.py`) designed for extracting articles from `.docx` documents. Key features include:
    *   **Robust Language Detection:** Functions like `is_zh_para` use confidence scoring based on Chinese character ratio and position.
    *   **Metadata Extraction:** Extracts English/Chinese titles, topics, URLs (categorized into outbound, image, file), and dates (with `validate_date`).
    *   **Content Processing:** Separates English and Chinese content, handles embedded image extraction (converting to base64 with a 10MB size limit), and normalizes strings for topics.
    *   **Standardized Output:** Assembles extracted data into a structured dictionary (`create_article_dict`) with extensive metadata fields (source, publish_date, host, url, mode, type, subtype, titles, topics, entities, persons, locations, coordinates, sentiment).
    *   **Centralized Logging:** Integrates with a `logging_setup` module, falling back to basic logging if unavailable.
    *   This file appears to be a more mature and feature-rich component, likely intended for production use.

2.  **`d:\air\rough.py` (Multiple Timestamps: 12/1/2025, 1:53:13 PM - 2:26:12 PM)**
    This file shows an iterative development process for a separate DOCX parsing script, moving from logging previous errors to implementing a more sophisticated extraction logic.

    *   **1:53:13 PM:** Contains a long log of "No articles extracted from: ..." messages, along with "File is not a zip file" and "Image too large" errors, indicating failures during a previous batch processing run on a large dataset of `.docx` files.
    *   **1:55:34 PM:** The entire previous error log content is commented out, suggesting the developer preserved the diagnostic information while moving on to new code.
    *   **2:09:59 PM:** Introduces a new, complete Python script. This version uses heuristic-based detection for articles, topics, and metadata within paragraphs. It features `is_chinese_char`, `detect_language` (with a 10% Chinese character threshold), `clean_text`, `extract_meta_from_block`, `normalize_topic`, `parse_docx` (to segment into raw article blocks based on numbered lists and topic headers), and `process_to_jsonl` (to structure data, including explicit "---TRANSLATED CONTENT---" separation or paragraph-by-paragraph language classification). It targets a small set of test files and outputs to `extracted_articles.jsonl`.
    *   **2:10:12 PM:** A minor path update for one input file (`data/SplTC_20250407_14.docx`).
    *   **2:11:06 PM:** Path adjustments to `INPUT_FILES` using raw strings (for Windows compatibility) and changed `OUTPUT_FILE` to `r"outputs\erred_articles.jsonl"`, suggesting a focus on handling errors or collecting results from specific problematic extractions.
    *   **2:11:21 PM:** Further refined `OUTPUT_FILE` path to `r"outputs\err\erred_articles.jsonl"`.
    *   **2:12:42 PM:** Added `os.makedirs(os.path.dirname(OUTPUT_FILE), exist_ok=True)` to ensure the output directory exists, improving robustness.
    *   **2:21:40 PM:** A major refactoring. The language detection threshold was lowered to 5% Chinese characters, and it now uses `valid_chars`. A more aggressive `clean_text_strict` function was introduced. Crucially, the parsing strategy shifted to a **TOC-driven approach** with a dedicated `parse_toc` function to map article numbers, titles, and topics, and then `parse_docx_toc_driven` to extract content guided by this TOC. The output JSON structure was simplified. The `OUTPUT_FILE` was temporarily changed to `"extracted_articles_v2.jsonl"`.
    *   **2:22:26 PM & 2:23:03 PM:** The `OUTPUT_FILE` path reverted back to `r"outputs\err\erred_articles.jsonl"`, likely for consistent error logging or specific testing runs. No functional code changes in these two entries.
    *   **2:26:12 PM:** The most refined version of `rough.py`. It further optimizes `clean` and `detect_language` utilities. The `extract_toc` function (renamed from `parse_toc`) is made more robust in identifying TOC boundaries and article entries (specifically looking for numbered lines *without* page dots to mark the content start). The `extract_articles` function (building on `parse_docx_toc_driven`) now clearly leverages the `toc_map` to segment and process articles, and the final output JSON structure is simplified to `id`, `title_en`, `topic`, `content_en`, `content_zh`.

### Timestamps of Significant Changes:

*   **12/1/2025, 1:46:07 PM:** Initial comprehensive `extractor_updated.py` module.
*   **12/1/2025, 2:09:59 PM:** Introduction of a new, standalone article extraction script in `rough.py`.
*   **12/1/2025, 2:12:42 PM:** Addition of output directory creation in `rough.py` for robustness.
*   **12/1/2025, 2:21:40 PM:** Major architectural shift in `rough.py` to a TOC-driven parsing strategy, with improved cleaning and language detection.
*   **12/1/2025, 2:26:12 PM:** Further refinement of the TOC-driven parsing and article extraction logic in `rough.py`.

### Patterns and Recurring Elements:

*   **DOCX Processing:** Both `extractor_updated.py` and `rough.py` are dedicated to extracting information from `.docx` files, primarily using the `python-docx` library.
*   **Bilingual Content Handling:** A consistent requirement is the ability to detect and separate English (`en`) and Chinese (`zh`) text, with functions like `is_chinese_char` and `detect_language` appearing in various forms across the scripts.
*   **Metadata Extraction:** Both approaches aim to extract titles, dates, URLs, and topics, though the specific fields and depth of extraction vary.
*   **Structured Output:** The goal is to produce structured data (JSONL format) from unstructured or semi-structured document content.
*   **Error Handling and Robustness:** Early `rough.py` logs show common document parsing issues. Later versions introduce explicit `os.makedirs`, better text cleaning, and more resilient parsing logic (especially the TOC-driven approach) to address these.
*   **Iterative Refinement:** The multiple `rough.py` entries within a short period highlight a rapid iterative development cycle, with continuous improvements to parsing logic, text cleaning, and path management. The move from simple heuristics to a TOC-driven approach in `rough.py` indicates a search for more reliable extraction.
*   **Two Distinct Implementations:** There are two different codebases for extraction (`extractor_updated.py` and `rough.py`), suggesting either independent development paths, or `rough.py` being a testbed for new ideas that might eventually be integrated into a more stable `extractor_updated.py` module.