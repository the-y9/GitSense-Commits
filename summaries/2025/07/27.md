# Activity Summary for 7/27/2025

## 11:49:40 AM
The log shows a series of changes primarily focused on creating and running a Docker image for a FastAPI application.

The `Dockerfile` underwent several revisions between 10:51 AM and 10:51 AM on July 27, 2025.  The key changes involved the `COPY` instruction: initially, it was `/app` (possibly incorrect), then it was corrected to `.` to copy the current directory.  The `requirements.txt` file was also renamed to `req.txt`.  The Dockerfile itself remained consistent in its core functionality, defining a Python 3.10 slim image, setting a working directory, installing dependencies, exposing port 8200, and running the uvicorn server for the iris_fastapi application.

At 11:10:27 AM, the `iris_fastapi.py` file was updated. This file contains a FastAPI application that loads a machine learning model (`model.joblib`) and provides an API endpoint for predictions.

Between 11:25 AM and 11:27 AM, the `week6_docker.txt` file, which appears to be a log of docker commands, was modified.  The log shows multiple attempts at building and running the `iris-api` image, including starting and stopping containers and checking their status using commands like `docker build`, `docker run`, `docker logs`, `docker ps`, and `docker stop`.  There's a repeated pattern of building the image and running it in detached mode, suggesting iterative development and debugging of the docker setup. The user also appears to be repeatedly adding their user to the docker group.  The final version of this log file appears more streamlined than its earlier version, possibly reflecting a cleaning-up or summarizing action.


## 3:04:44 PM
The log shows development of a FastAPI application for iris species classification.  On July 27th, 2025, at 2:07:10 PM, `iris_fastapi.py` was created, implementing a simple API using FastAPI, pydantic, joblib, and pandas.  This script loads a pre-trained model (`model.joblib`) and provides an endpoint to predict iris species based on sepal and petal measurements.

Subsequently, at 2:08:32 PM, a `week6_docker.txt` file logged the process of building and running a Docker container for this application.  The log shows multiple instances of building the image (`docker build`), running the container (`docker run`), viewing logs (`docker logs`), and managing containers (`docker ps`, `docker stop`). This suggests iterative development and testing of the dockerized application.  The repeated build and run commands indicate potential debugging or refinement of the Dockerfile (not shown in the log).
