# Activity Summary for 7/31/2025

## 2:54:41 PM
The README.md file was updated twice on July 31, 2025, with the second update adding a data link.  The initial README details setting up Ollama locally using WSL, creating a Python virtual environment, and using the `summarize_json.py` script to process a JSONL file of articles. This script uses the Ollama Mistral model with a generator function for efficient summarization, including robust error handling. The script parameters (`num_ctx`, `temperature`, `top_k`, `top_p`, `repeat_penalty`) are clearly documented.  Summaries are saved to `outputs/summary_{model}.txt`.

A `.gitignore` file was also modified multiple times on July 31, 2025, adding `sample_data/` to the ignored paths. This file excludes various files and directories from Git version control, including temporary files, virtual environment directories, and specific Python scripts.

The `performance.md` file, updated twice on July 31, 2025, documents the performance testing results of different models (LLaMA2:7B, Mistral, Mixtral) on a Dell machine with an NVIDIA GeForce GTX 1650 Ti GPU and 15.77 GB RAM.  The timings show significant differences based on model and batch size. Mixtral failed due to insufficient RAM. The final version of `performance.md` presents the results in a more organized table format.  The commit message indicates changes to `README.md`, `outputs/may/summary_mistral.txt`, `performance.md`, `pipeline.py`, and `summarizer/summarize_json.py`.


## 3:54:42 PM
The `chatbot.py` file underwent several revisions on July 31st, 2025.  The core functionality remained consistent throughout, focusing on a chatbot that retrieves information from a ChromaDB database (`class10_english` collection) and interacts with an LLM (initially OpenAI, later exclusively Ollama).

The first significant change (3:08:23 PM) involved setting up the chatbot to use either OpenAI or Ollama, depending on the `USE_OPENAI` flag.  The code included helper functions for embedding queries, retrieving relevant document chunks from the database, querying the chosen LLM (OpenAI's `gpt-3.5-turbo` or Ollama's `llama3.1`), and streaming responses from Ollama.  Two examples demonstrate querying the system for information about a poem.

The second revision (3:09:57 PM) removed the OpenAI-related code entirely, making Ollama the sole LLM used. This simplification removed the `USE_OPENAI` flag, `OPENAI_MODEL`, and the `ask_openai` function.  The example now exclusively uses `ask_ollama`.

The final update (3:19:20 PM) involved a minor change in the `persist_path` for ChromaDB, changing it from `../chroma_db` to `/chroma_db`.  No functional changes were made to the chatbot's logic or interaction with the LLM.  Throughout the revisions, the core structure—embedding, retrieval, LLM interaction, and response handling—remained unchanged. The use of Ollama as the LLM and the `class10_english` collection in ChromaDB persisted as constants.


## 5:54:42 PM
The log shows modifications to a chatbot application using ChromaDB for vector search and Ollama for an LLM.

The initial `chatbot.py` (5:01:17 PM)  establishes a chatbot that retrieves relevant text chunks based on user queries, uses these chunks as context for an Ollama LLM ("llama3.1"), and displays the response.  The code includes functions for embedding text using `sentence-transformers`, querying ChromaDB, interacting with the Ollama API, and streaming the LLM's response.  Two examples demonstrate querying for a poem and then displaying the full poem.


A `.gitignore` file was created at (5:19:53 PM), adding several files and directories to the ignore list, including previous versions of the `chatbot.py` file, suggesting iterative development.

Finally,  `chatbot.py` was modified again at (5:20:09 PM).  The code remains functionally identical to the initial version.  This suggests that the second commit might have been accidental or a minor edit that didn't change the core functionality.
