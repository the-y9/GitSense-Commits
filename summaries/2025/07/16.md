# Activity Summary for 7/16/2025

## 9:40:16 PM
The log shows extensive modifications to `d:\air\summarize_json.py`, focusing on refining a JSON summarization function using the Ollama API.  Initially, the script loaded JSON from `output_articles.jsonl`, then switched to `sample.json`.  There were several attempts to write the summary to an output file, with various levels of success.  Between 8:55 PM and 9:11 PM, the code underwent significant changes, primarily involving error handling and improving the prompt engineering. The script initially used `ollama.chat`, but was later updated to use `ollama.generate` around 9:01 PM.  This improved the API interaction and allowed for more control over the generation process, including the addition of parameters like `num_ctx`, `temperature`, `top_k`, `top_p`, and `repeat_penalty` between 9:02 PM and 9:03 PM. The function was also enhanced to accept optional `user_prompt` and `system_prompt` parameters. The structure of the prompt was altered at 9:09 PM and subsequently improved by adding error handling around 9:11 PM and then the addition of a system prompt around 9:18 PM.  The most stable version includes a `try-except` block for robust error handling and the ability to select from multiple user prompts.  Finally, a system prompt was introduced at 9:18 PM, setting the context for the summarization model, and the input file was consistently  `sample.json`, outputting the results to `outputs/summary_mistral.txt`.

`d:\air\doc-to-json\doc_to_json.py` was modified once at 8:54:46 PM. This script extracts data from a docx file (`EOI - Negative_Economist_views.docx`), specifically English and Chinese titles, URLs, content, and dates, then saves it as JSONL into `outputs/output_articles.jsonl`.  The script uses regular expressions for various pattern matching and content extraction, including for date identification.


The `d:\air\rough.py` file contains experiments with prompt engineering for the summarization task, showing different versions of constructing the system and user prompts. The newline character used in the prompt was corrected around 9:15 PM.  These changes happened around 9:15 PM and 9:16 PM. A final version of the system and user prompt is present at the end of the logs.

The `.gitignore` file was updated at 9:14 PM to ignore specific files and directories during Git version control.
