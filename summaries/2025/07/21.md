# Activity Summary for 7/21/2025

## 12:15:52 PM
The log shows several modifications to the `.gitignore` file between 11:18 AM and 11:23 AM on July 21, 2025, primarily focused on refining the exclusion patterns for temporary files and directories.  The changes consistently involve `.venv`, `summarizer` subdirectories (containing `summarize_vllm.py` and `pdf_summarization.py`), a `rough.py` file, a JSONL file, `temp_input`, and `__pycache__` directories.  The final `.gitignore` entry also excludes a `data` directory.

Significant changes to the codebase occurred between 11:27 AM and 11:34 AM on July 21, 2025, affecting `pipeline.py` and `doc_to_json.py`.

`pipeline.py` (11:27 AM and 11:30 AM): This Python script processes either a ZIP file or a directory containing `.docx` files.  It extracts text from the `.docx` files, creates a JSONL file (`output_articles.jsonl`), and then uses a summarizer (`summarizer/summarize_json.py`) to generate summaries. The script handles different input types (ZIP, directory, single .docx file) and includes error handling and temporary directory cleanup. The script was updated in two separate commits, with no functional changes identified in the content comparison.

`doc_to_json.py` (11:30 AM, 11:31 AM, and 11:34 AM): This script extracts information from `.docx` files, identifying English and Chinese titles, URLs, and content. It uses regular expressions to find dates.  The script writes the extracted data into a JSONL file. The output path was corrected in a commit at 11:31 AM, changing from `doutputs/output_articles.jsonl` to `outputs/output_articles.jsonl`.  Another commit at 11:34 AM shows no further content changes.


## 2:16:59 PM
The log shows the development of a Python script (`machine.py`) to gather system information and a pipeline (`pipeline.py`) for processing and summarizing documents.

`machine.py` underwent several revisions between 1:17 PM and 1:25 PM on July 21, 2025.  Initially, it only printed basic machine information (machine name, CPU, OS, and GPU).  Subsequent changes added RAM information, first using a simplified method based on the Python version, then using the `psutil` library for a more accurate measurement.  Finally, disk space information was incorporated using the `shutil` library.  The GPU information retrieval was also enhanced to include VRAM details. The final version included a `print_sys_info()` function to organize the output.


`pipeline.py` was modified between 1:26 PM and 1:31 PM on July 21, 2025. This script processes .docx files (or zip archives containing them) by extracting text, converting it to JSONL format, and then using a summarization model (initially 'mistral', later changed to 'llama2:7b').  The key changes include adding a progress bar and error handling, and the addition of a timer to measure execution.  A significant change introduced an `output_dir` parameter to allow specification of the output directory for results, improving flexibility. The final version includes the addition of time tracking for the entire script execution.

`summarizer/summarize_json.py` (timestamp 1:27 PM) is a summarization script that takes a JSONL file as input, processes each article, and produces summaries using the `ollama` library. It uses the Mistral model by default but allows for model specification via command-line arguments.  This script is called by `pipeline.py`.

`extractor/doc_to_json.py` (timestamp 1:27 PM) extracts information from .docx files, creating a JSONL file containing extracted data like title, content, and date. This script uses regular expressions to identify and extract relevant information from the docx file, handling both English and Chinese text.


The `.gitignore` file (timestamp 1:26 PM) lists files and directories to be excluded from version control.

Finally, `performance.md` (updated between 1:32 PM and 1:36 PM) records performance metrics, including machine specifications, model used, document size, and summarization success/failure.  The log shows that there was an issue running the `Mixtral` model due to insufficient system memory.
