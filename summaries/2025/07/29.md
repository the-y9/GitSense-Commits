# Activity Summary for 7/29/2025

## 10:36:04 PM
The log shows the development of a FastAPI application (`demo_log.py`) for machine learning predictions, deployed on Google Kubernetes Engine (GKE).

On July 29th, 2025, the `deployment.yaml` file (9:38 PM) defines a deployment with two replicas of a containerized FastAPI application, using a service account named `telemetry-access`.  The container uses a custom image and exposes port 8200, including readiness and liveness probes.

The `gcloud commands for week 7.txt` file (10:10 PM) details the deployment process.  This includes enabling relevant Google Cloud services (Container, Logging, Monitoring, Cloud Trace), building and pushing a Docker image, creating a GKE cluster (`demo-log-ml-cluster`), and configuring the `telemetry-access` service account with necessary IAM permissions for logging and Cloud Trace.  The file also outlines steps for confirming the serviceaccount's existence and provides commands for applying deployment, service, and HPA configurations, checking the deployment status, restarting deployments, and performing load tests.

The `requirements.txt` file (10:23 PM) lists the application's dependencies, including FastAPI, scikit-learn, and OpenTelemetry for tracing.

The `demo_log.py` file undergoes several revisions (between 10:32 PM and 10:35 PM). Initially, it's a complete FastAPI application utilizing OpenTelemetry for structured logging and Cloud Trace integration.  Later revisions remove the OpenTelemetry integration, simplifying the logging and tracing aspects. The application includes liveness and readiness probes, structured logging, exception handling, and a `/predict` endpoint.  The application loads a model (`model.joblib`) on startup.  A key change involves switching from directly using `dummy_model` to using `make_prediction` which utilizes the loaded model. The final version of `demo_log.py` removes all OpenTelemetry related code, leaving only structured logging and error handling.


## 11:36:08 PM
The log shows iterative development of a FastAPI application (`demo_log.py`) and its deployment to a Google Kubernetes Engine (GKE) cluster.

The `demo_log.py` file underwent several revisions between 10:39 PM and 10:46 PM on July 29, 2025.  Key changes include:

* **Input Schema Refinement:** The initial version used a generic `Input` class. This was later replaced with a more specific `IrisInput` class, suggesting the application's purpose is likely related to Iris flower classification.

* **`make_prediction` Function Modification:** The `make_prediction` function's input type changed from `Input` to `dict` and then finally to `IrisInput`, reflecting the change in input schema.  Additionally, the unnecessary `input.dict()` call in the `predict` function was removed in the final version.

* **Removal of OpenTelemetry Instrumentation:**  The commented-out OpenTelemetry code suggests an initial attempt at integrating tracing and distributed logging.  This was eventually removed in the final version of the code, possibly due to configuration issues or a decision to use alternative logging mechanisms.


* **Logging Enhancements:** The logging system was set up to output JSON-formatted logs. The structure and level of detail in the logged messages improved over time, including recording latency and request details.


* **Error Handling:** Comprehensive exception handling was implemented using FastAPI's `exception_handler` and logging error details.

The deployment (`deployment.yaml`) specifies a deployment of two replicas of the `demo_log-ml-service`, using a custom image  `us-central1-docker.pkg.dev/eternal-cycling-463617-f7/iris-repo/demo_log:latest`. Readiness and liveness probes were configured using `/ready_check` and `/live_check` endpoints respectively.


The `gcloud commands for week 7.txt` file documents the steps for deploying the application to GKE.  This includes:

* Enabling necessary Google Cloud services (Container, Logging, Monitoring, Cloud Trace).
* Building and pushing the Docker image.
* Creating a GKE cluster (`log-ml-cluster` in the final version) with workload identity and logging/monitoring enabled.
* Creating and configuring a service account (`telemetry-access`) with appropriate IAM roles for logging and Cloud Trace.
* Applying Kubernetes manifests (deployment, service, and horizontal pod autoscaler (HPA) – not shown in the log but mentioned).
* Instructions for verifying deployment and service status, including using probes, checking pod statuses, and using `curl` to test the `/predict` endpoint.
* Performance testing using `wrk`.

The final version of the `gcloud commands` file shows that the cluster name was changed to `log-ml-cluster`, while the service and deployment names remained the same. There was also some reorganization of the steps.
