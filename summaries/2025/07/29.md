# Activity Summary for 7/29/2025

## 10:36:04 PM
The log shows the development of a FastAPI application (`demo_log.py`) for machine learning predictions, deployed on Google Kubernetes Engine (GKE).

On July 29th, 2025, the `deployment.yaml` file (9:38 PM) defines a deployment with two replicas of a containerized FastAPI application, using a service account named `telemetry-access`.  The container uses a custom image and exposes port 8200, including readiness and liveness probes.

The `gcloud commands for week 7.txt` file (10:10 PM) details the deployment process.  This includes enabling relevant Google Cloud services (Container, Logging, Monitoring, Cloud Trace), building and pushing a Docker image, creating a GKE cluster (`demo-log-ml-cluster`), and configuring the `telemetry-access` service account with necessary IAM permissions for logging and Cloud Trace.  The file also outlines steps for confirming the serviceaccount's existence and provides commands for applying deployment, service, and HPA configurations, checking the deployment status, restarting deployments, and performing load tests.

The `requirements.txt` file (10:23 PM) lists the application's dependencies, including FastAPI, scikit-learn, and OpenTelemetry for tracing.

The `demo_log.py` file undergoes several revisions (between 10:32 PM and 10:35 PM). Initially, it's a complete FastAPI application utilizing OpenTelemetry for structured logging and Cloud Trace integration.  Later revisions remove the OpenTelemetry integration, simplifying the logging and tracing aspects. The application includes liveness and readiness probes, structured logging, exception handling, and a `/predict` endpoint.  The application loads a model (`model.joblib`) on startup.  A key change involves switching from directly using `dummy_model` to using `make_prediction` which utilizes the loaded model. The final version of `demo_log.py` removes all OpenTelemetry related code, leaving only structured logging and error handling.
