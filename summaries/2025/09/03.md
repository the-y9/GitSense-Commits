# Activity Summary for 9/3/2025

## 4:19:23 PM
The log shows development of a Python script to extract information from a PDF of the Bhagavad Gita ("BG_As_It_Is.pdf") and output it as JSONL.  The process started with experimentation in `rough.py` around 3:30 PM on September 3rd, 2025. This involved initial text cleaning and transliteration functions, using libraries like `indic_transliteration` and `re` for regular expression matching.

Around 3:31 PM,  `rough.py` and `sanitize.py` were added to the `.gitignore` file, suggesting a shift towards organizing the project.  The `rough.py` file then underwent several iterations, focusing on PDF processing using `pdfplumber`.  The script initially aimed to simply extract and print text,  then evolved to identify a termination phrase ("Thus end the Bhaktivedanta Purports to the Eighteenth Chapter") to control the extraction process.


A separate file, `extract1.py`, was created at 3:39 PM to focus on extracting chapter, shloka, synonyms, translation and purport sections. The code extensively uses regular expressions to identify these sections in the extracted text.  This file underwent significant changes involving refinement of regular expressions for chapter identification (`is_chp` function).  There was considerable debugging of the regular expression throughout the period of 3:39 PM to 4:05 PM. The initial attempts to extract chapter numbers from the PDF failed because the initial regular expression was not accurate and there were issues with the loop handling.

By 4:08 PM, the goal shifted to creating a JSONL output, storing extracted data in a list of dictionaries (`obj`). The structure of the dictionary was refined between 4:09 PM and 4:12 PM to include "chapter", "shloka", "synonyms", "translation", and "purport" keys.  The script was further improved by including a progress bar using `rich.progress` starting at 4:11 PM. Finally, by 4:17 PM, the script successfully processed the PDF and produced a JSONL file. The final version successfully handles each section separately, resulting in a structured JSONL output for each chapter.  The inclusion of error handling and progress reporting indicates a focus on robustness and user experience.


## 5:19:22 PM
The log shows development of a Python script (`extract1.py`) to extract data from a PDF ("content/BG_As_It_Is.pdf") and save it as JSONL ("content/BG_As_It_Is.jsonl").  A parallel `rough.py` file tracks experimental regex and PDF processing techniques.


**`extract1.py` Evolution:**

The initial version (4:42:36 PM) uses `pdfplumber` to iterate through PDF pages, extracting text and using regular expressions to identify chapters and shlokas.  It stores the extracted information (chapter number, shloka number, synonyms, translation, purport) in a list of dictionaries and writes it to a JSONL file. The progress bar is implemented using `rich.progress`.

Subsequent changes (5:14:31 PM and later) refactor the code to improve page processing.  The original approach iterated through lines within each page, but it was changed to loop through pages directly, improving efficiency. There's an attempt to integrate the chapter and shloka identification more smoothly.  However,  the final versions still contain logic errors related to properly updating `shl`, `syn`, `trans`, and `purport` values. The `page_num` variable is also not correctly updated in the progress bar in the final versions.


**`rough.py` Evolution:**

This file documents the iterative refinement of regular expressions to identify shloka numbers (e.g., "TEXT 18") from the PDF's text content.  Multiple revisions adjusted the regex pattern (`re.match` vs. `re.search`), quantifiers (`\d+`, `\d{1,2}`), and incorporated more sophisticated logic to check only the first occurrence in each page.  The final version (5:11:28 PM) shows a more optimized approach that iterates over lines rather than the entire page text at once. It demonstrates various attempts to efficiently find and extract shloka numbers.

**Key Timestamps:**

* **4:42:36 PM:** Initial `extract1.py` version.
* **4:44:09 PM - 4:48:57 PM:**  Series of experiments in `rough.py` to perfect regex for extracting shloka numbers, gradually evolving into more robust solutions using `pdfplumber`.
* **5:14:31 PM - 5:16:27 PM:**  Significant refactoring of `extract1.py` to process pages individually and optimize page processing.

**Recurring Elements:**

* Consistent use of regular expressions (`re` module) to parse text.
* Extensive use of `pdfplumber` for PDF processing.
* Focus on extracting chapter and shloka numbers from the PDF.
* Iterative development process reflected in frequent commits to both files.
