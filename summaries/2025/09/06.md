# Activity Summary for 9/6/2025

## 12:26:05 PM
The log shows modifications to the file `c:\Users\dell\Desktop\projects\py docs\grade.py`.  The file contains Python code that calculates and grades scores for three subjects: "Gen ai," "mlops," and "gt."  Each subject uses a different weighted formula to compute a total score.  A function `p(n)` then assigns a letter grade and a numerical value based on this total score.

The main change between the two timestamps (9/5/2025, 9:48:13 PM and 9/5/2025, 9:48:31 PM) is a minor update to the final calculated score and grade for the "mlops" subject. Initially, the mlops score was approximately 56, resulting in a grade of 'D'.  After the update, the mlops score changed to approximately 59, still resulting in a 'D' grade.  The code itself, including the grading function and formulas for each subject, remained identical between the two versions.  The only difference lies within the calculated results presented in the docstrings.


## 1:38:10 PM
The code consists of a Python script (`multi_doc_jsonl.py`) that processes a zip file containing DOCX documents.  The script extracts the DOCX files, converts their content to JSONL format, and saves the resulting JSONL files to a specified output directory.

The key changes between the two commits are minimal;  the only significant difference is the change in the `zip_path` variable.  The first commit uses `"data/SI-2022.zip"`, while the second commit updates this to `"data/SI-2023.zip"`.  This suggests that the script is designed to be reusable for different zip files, likely containing data from different years (2022 and 2023).  Both versions of the script were modified on September 6th, 2025, with the second revision occurring approximately 7 minutes after the first.

The script's functionality remains unchanged otherwise: it uses logging for debugging and progress reporting, handles potential exceptions during file extraction and conversion, and provides a summary of the processing including the number of files processed, articles extracted, and any failures encountered.  The `doc_to_json` module is used for the actual DOCX to JSONL conversion.


## 2:38:13 PM
The log shows a series of modifications to the `doc_to_json.py` file between 1:52 PM and 2:36 PM on September 6th, 2025.  The primary focus of these changes is enhancing the extraction of information from DOCX files, specifically adding the capability to extract and encode embedded images.

Initially, `doc_to_json.py` parsed DOCX files to extract metadata and content (English and Chinese), URLs, and dates.  The output was a JSONL file containing structured data for each extracted article.

Between 1:52 PM and 2:30 PM, the modifications to `doc_to_json.py` involved minor adjustments to the logic, primarily how it handles multiple consecutive `Topic` paragraphs and concatenates their text.  There was also a change in the input file path used in the `__main__` block, transitioning from `data\SI-2023.zip\SI-2023\FP_formate.docx` to `data\FP_formate.docx` and then to `data\CHAT-GPT AND INFORMATIZATION BY PLA_20230420_21.docx`.


The most significant changes occurred between 2:30 PM and 2:36 PM.  A new function, `extract_image_from_run`, was added to extract embedded images from runs within paragraphs.  These images are then encoded as base64 strings and added to the output JSONL under the `images` key.  A previous attempt to extract images using `extract_embedded_images_as_base64` was removed.  The `images` key in the output JSON was previously empty; after these changes, it contains the base64 encoded image data.  The input file path in the `__main__` section remained consistent during this period, focusing on processing `data\CHAT-GPT AND INFORMATIZATION BY PLA_20230420_21.docx`.

The `multi_doc_jsonl.py` file, modified at 1:40 PM, describes a pipeline that processes a ZIP archive of DOCX files. It uses the `doc_to_json.py` module to convert each DOCX to JSONL format, handling potential errors and providing a progress indicator. This file remained unchanged after its initial commit.  The pipeline outputs processed data into the `outputs/si_outputs` directory.  The main function checks for a valid ZIP file before initiating the process.


## 3:38:20 PM
The log shows a Python script (`doc_to_json.py`) undergoing development to extract data from docx files and a second script (`multi_doc_jsonl.py`) that processes a zip archive of docx files.

`doc_to_json.py`  underwent significant changes primarily focused on improving the extraction of image URLs from docx files. The initial version (2:47 PM) included  functions to identify English and Chinese paragraphs, extract dates and images (as base64 encoded strings), and  construct a JSON object containing extracted data.  Subsequent revisions (2:58 PM, 2:59 PM) added  `extract_image_url_from_run` to handle hyperlinked images. This function was refined further in later commits (3:01 PM, 3:01 PM, 3:02 PM, 3:02 PM, 3:03 PM) to improve accuracy and robustness of  hyperlink extraction from image runs, addressing an "Invalid expression" error. Finally, at 3:11 PM, the script added `EN_TITLE_STYLES` tuple to improve the identification of article titles based on docx style names and added more style names into the tuple in subsequent updates (3:13 PM, 3:13 PM, 3:14 PM).  The final version (3:20 PM, 3:28 PM) integrated logging using the `logging` module to improve debugging. The log file is changed to `si_docs.log` at 3:28 PM.  Error handling for document opening was improved at 3:18 PM.

`multi_doc_jsonl.py` remained relatively stable. Its purpose is to process a zip file (`data/SI-2023.zip`), extract docx files, use `doc_to_json.py` to convert each to JSONL format, and save the results. This script uses `rich` for progress display and `logging` for recording the process, including successful and failed conversions. The log file `si_pipeline.log` (recorded at 3:36 PM) shows that the script successfully processed many files but some docx files resulted in "No content extracted" warnings, indicating issues with the content of those specific files or problems in `doc_to_json.py`.  The log entries show the number of items saved to each jsonl file.

The overall pattern shows iterative development of the `doc_to_json.py` script to improve image URL extraction, followed by stable usage of this script within the `multi_doc_jsonl.py` pipeline.  The use of logging in both scripts facilitates debugging and monitoring of the entire process.


## 5:48:25 PM
The log shows multiple updates to configuration files for a LinkedIn job application bot.  The `personals.py` file (4:57:52 PM) was initialized with personal information for the bot's application process, including name, contact details, and answers to equal opportunity questions.  The `questions.py` file underwent several revisions (4:58:00 PM, 5:04:12 PM, 5:05:46 PM, 5:06:54 PM, 5:14:09 PM, 5:39:23 PM, 5:40:36 PM) with modifications to various parameters.  These changes primarily involved updating the user's LinkedIn profile, portfolio link, years of experience, desired salary, current CTC, notice period, LinkedIn headline and summary, and citizenship status.  The `years_of_experience` variable was changed from "5" to "1" and then remained at "1". The `us_citizenship` variable was changed from "U.S. Citizen/Permanent Resident" to "Indian" and then to "Other". The `website` and `linkedIn` fields were updated multiple times reflecting changes to the user's online presence.  The `desired_salary` value was changed from 1200000 to 1400000 and the `current_ctc` variable changed from 800000 to `None` and then to 0. The `linkedin_headline` and `linkedin_summary` were significantly changed to reflect the user's updated professional profile at 5:14:09 PM. The `recent_employer` was also updated.  The `resume.py` file (5:15:10 PM) was created or updated to manage resume settings.  The `search.py` file (5:19:20 PM, 5:23:36 PM) was modified to define job search preferences, including keywords, location, and filters, specifically adding "Entry level" to the `experience_level` list at 5:23:36 PM and adding "JAVA" to `bad_words`. The `secrets.py` file underwent numerous changes (5:23:46 PM, 5:24:29 PM, 5:25:25 PM, 5:26:27 PM, 5:26:33 PM, 5:26:42 PM, 5:27:04 PM, 5:28:05 PM, 5:29:47 PM, 5:46:29 PM) primarily focusing on integrating environment variables for LinkedIn credentials and AI settings.  Initially, the credentials were directly added to the code.  Later, the file was refactored to load credentials and AI-related information from a `.env` file using `load_dotenv()` and `os.getenv()`, improving security. The `use_AI` variable was changed from `True` to `False` in one update and the `llm_api_url` was set to a specific OpenAI API endpoint. The `.gitignore` file was updated at 5:27:18 PM to exclude sensitive files and folders from version control.  The `.env` file also had two updates changing the environment variable names. A new `log.txt` file was created, but it appears to be empty.  Finally, the `settings.py` file (5:31:56 PM, 5:37:30 PM) was updated, adding or modifying various settings like whether to close browser tabs after application, and enabling the `generated_resume_path` variable.

Overall, the log shows a continuous refinement and development of the LinkedIn job application bot.  The most significant changes are related to improving security by using environment variables (secrets.py and .env files), enhancing user profile information (questions.py), and defining specific job search criteria (search.py).
