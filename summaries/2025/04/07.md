# Activity Summary for 4/7/2025

## 2:08:04 AM
The log shows a single file, `rough.py`, being modified repeatedly between 1:08 AM and 1:17 AM on April 7th, 2025.  The file uses `matplotlib` and `numpy` to generate a 3x2 grid of plots.  The main focus of the changes is on the first subplot, which spans two columns.

Initially, the code plots sine, cosine, and tangent functions across different subplots.  Subsequent edits to `rough.py` center around the first subplot:

* **1:11 AM:** An attempt to add a second line to the first subplot (`plt.plot(x, y2, c)`), which is incomplete.
* **1:12 AM:**  The incomplete line is fixed by specifying the color as `color='red'`. A legend is added.
* **1:12 AM:** The legend is improved by explicitly specifying labels (`plt.legend(['sin(x)', 'cos(x)'])`).
* **1:13 AM:** The labels in the legend are changed to 'Training Loss' and 'Validation Loss'.
* **1:14 AM:** A grid is added to the first subplot (`plt.grid(True)`).
* **1:15 AM:** The legend's location is specified as 'upper right' (`plt.legend(loc='upper right')`).
* **1:17 AM:**  A grid is added to the second and third subplots.


The modifications show a progressive refinement of the first subplot, enhancing its visual clarity and providing more informative labels for a likely machine learning context (Training Loss and Validation Loss).  The other subplots receive minor updates towards the end, adding a grid for consistency.


## 12:49:22 PM
The log shows two distinct Python scripts.

The first script, `rough.py` (4/7/2025, 12:08:04 PM),  is a simple matplotlib script that generates a 2x2 grid of plots.  It plots sine, cosine, and tangent functions, with the first plot spanning two columns. The script uses `plt.subplot` to arrange the plots and `plt.tight_layout` to adjust the layout.

The second script, `dlp_nppe3.py` (4/7/2025, 12:33:16 PM), is considerably more complex. It implements a SwinIR model for image super-resolution.  The script defines a custom SwinIR class, which is a convolutional neural network with transformer-like elements. It then loads image data from specified directories (`/kaggle/input/dlp-jan-2025-nppe-3/archive/train/`, `/kaggle/input/dlp-jan-2025-nppe-3/archive/val/`, `/kaggle/input/dlp-jan-2025-nppe-3/archive/test/`), trains the model using an Adam optimizer and MSELoss, and evaluates its performance.  Mixed precision training using `GradScaler` is employed to accelerate training. Finally, it generates predictions on a test set, saves these predictions as images to `/kaggle/working/test_predictions`, and converts them into a CSV file named `submission.csv` located at `/kaggle/working/submission.csv`.  The code utilizes libraries like `torch`, `torchvision`, `PIL`, `pandas`, and `numpy`.  A key feature is the use of custom `ImageDataset` class for efficient data loading.  The training involves 50 epochs.


## 1:49:21 PM
The log shows a single code commit at 4/7/2025, 1:41:52 PM, modifying `c:\Users\dell\Downloads\dlp_nppe3.py`.  This Python script trains and evaluates a SwinIR model for image super-resolution.  Key features include:

* **Model Definition:** A `SwinIR` class implements the Swin Transformer-based super-resolution model.  The model architecture is defined within this class, including convolutional layers, GELU activation, and pixel shuffle for upsampling.

* **Data Handling:**  The script uses custom `ImageDataset` class to load low-resolution (LR) and high-resolution (HR) image pairs from specified directories (`train_lr_dir`, `train_hr_dir`, `eval_lr_dir`, `eval_hr_dir`, `test_lr_dir`).  The data is then loaded into `DataLoader` objects for training and evaluation.

* **Training Process:** The script trains the model using Adam optimizer and Mean Squared Error (MSE) loss function. Mixed precision training using `GradScaler` is employed for efficiency.  PSNR and SSIM metrics are calculated and tracked during both training and validation.

* **Evaluation and Metrics:**  The script evaluates the model on a validation set and plots training/validation loss, PSNR, and SSIM curves over epochs.  PSNR and SSIM are computed using `skimage` library.

* **Inference:**  After training, the script generates super-resolution predictions on a test set, saving the output images to `/kaggle/working/test_predictions`.

The script utilizes PyTorch for deep learning,  NumPy and Pandas for data handling,  Scikit-image for image metrics, and Matplotlib for plotting.  The training is configured for 50 epochs with a batch size of 4.  The file paths point to a Kaggle dataset.


## 2:49:23 PM
The log shows modifications to two main Python files: `rough.py` and `dlp_nppe3.py`.  `rough.py` undergoes several revisions focused on plotting animations and data visualization using `matplotlib` and `IPython`.  The initial version (1:54:23 PM) creates a 2x2 subplot animation using `FuncAnimation`.  Subsequent changes (1:55:58 PM, 1:58:35 PM, 2:00:29 PM, 2:01:00 PM) refine this animation, switching to a frame-by-frame update with `clear_output` and `display` for inline Jupyter Notebook display, and experimenting with different animation methods.  Finally, versions at 2:06:01 PM and 2:09:27 PM modify the plotted functions, experimenting with different mathematical expressions for sine, cosine, and tangent, showing a focus on exploring different function behaviors.

`dlp_nppe3.py`, on the other hand, contains a more substantial codebase implementing a SwinIR model for image super-resolution.  This file remains largely consistent across its revisions (2:03:20 PM, 2:03:49 PM, 2:04:00 PM, 2:04:08 PM, 2:15:11 PM, 2:34:05 PM, 2:34:26 PM).  The core structure—defining the SwinIR model, creating image datasets, training the model using MSE loss and Adam optimizer, calculating PSNR and SSIM metrics, and generating predictions—is maintained. Key differences lie in how test predictions are handled and stored. Initially, attempts are made to save predictions directly as CSV. Later versions change to saving predictions as images in a designated output folder (`/kaggle/working/test_predictions2`) and then subsequently converting them into a CSV file named `submission.csv` using a `images_to_csv` function. The changes indicate a shift towards a more structured approach to handling the prediction output, improving the workflow for submitting results.  The final version at 2:34:26 PM removes the real-time plotting of training metrics, instead generating static plots after training completes.
